{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "permissions": {
    "deny": [
      "Edit(dist/**)",
      "Write(dist/**)",
      "Edit(.astro/**)",
      "Write(.astro/**)",
      "Edit(package-lock.json)",
      "Write(package-lock.json)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "statusMessage": "改动计算逻辑,跑 Vitest…",
            "command": "f=$(jq -r '.tool_input.file_path // empty'); case \"$f\" in *src/lib/*|*tests/*) if ! out=$(npm test 2>&1); then echo \"❌ HijriTools 测试未通过——改动计算逻辑后必须 GREEN(铁律:测试是正确性的唯一保证):\" >&2; echo \"$out\" | tail -30 >&2; exit 2; fi ;; esac"
          }
        ]
      }
    ]
  }
}
